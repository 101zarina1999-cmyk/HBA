workflows:
  android-build:
    name: Build Android APK
    instance_type: mac_mini_m1
    scripts:
      - name: Build Android APK
        script: |
          gradle assembleDebug
    artifacts:
      - app/build/outputs/**/*.apk